# Thread-Program
Program to understand how to use threads in a c program

Include the standard libraries below in your c file
stdio.h
sttlib.h
unistd.h
pthread.h

(NOTE! When you compile your thread program, use the -g and -pthread arguments with your compiler example: gcc -g -pthread  yourcode.c)
